import { WICConfig } from "@/types/models";

/**
 * The default WIC config.
 */
export const DEFAULT_CONFIG = {
  sidebarMode: 0,
  notificationLevel: 4,
  imageFormat: 'image/jpeg'
} as WICConfig;

export const MIME_TYPE_BINARY = 'application/octet-stream';

export const ENCRYPTION_EXT_NAME = '.enc';

